import styled from '@emotion/styled';
import { Theme } from '@mui/material';
import { Input } from 'components/ui';

export const SLocalizationSelectMain = styled(Input)<{ theme?: Theme }>`
  ${({ theme }) => ``}
`;
