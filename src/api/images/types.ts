export type TImageName = {
  name: string;
};

export type TImageId = {
  id: number;
};

export type TImage = {
  id: number;
  name: string;
  key: string;
  houseId: number;
  createdAt: string;
  updatedAt: string;
};
