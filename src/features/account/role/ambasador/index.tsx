import React from 'react';

import { AccountMain } from 'features/account/style';

const AccountPage = ({ ...props }) => (
  <AccountMain {...props}>
    <div>Bla bla</div>
  </AccountMain>
);

export default AccountPage;
